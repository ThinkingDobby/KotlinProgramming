package chap03.section1
/*
fun main() {
    normalVarargs(1, 2, 3, 4, )
    normalVarargs(4, 5, 6)  //Kotlin: The feature "trailing commas" is only available since language version 1.4
}

fun normalVarargs(vararg a : Int) {
    for (i in a) {
        println("$a")
    }
    println("\n")
}
 */